package com.vaibhav.lld.design.SchemaDesign.SnakesLadder.v1;

public class LadderV1 {
    private int from;
    private int to;
    // code duplication in ladder and snake class as same fields
    // so we can abstract this out code duplication and both these classes can extends from here
}
